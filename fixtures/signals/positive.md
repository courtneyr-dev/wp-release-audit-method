# Ecosystem notes fixture — for scripts/ecosystem-signals.sh --self-test
# One clean line per signal class, plus one noise line, so each classifier is
# verified independently. Real notes produce compound tags; that is expected.

WordPress 7.2 beta 1 is scheduled, with a change to the upload path in the dev notes.
WP Rocket released a new version with revised Cloudflare settings this week.
The 7.0.3 security release is worth reviewing against every build we ship.
A plugin caused a fatal error on 6.9 upgrades for sites using Imagick.
This line is ordinary release-notes prose about nothing in particular and should not flag.
